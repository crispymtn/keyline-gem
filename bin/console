#!/usr/bin/env ruby

require "bundler/setup"
require "keyline"

if ENV.has_key?('KEYLINE_API_HOST') && ENV.has_key?('KEYLINE_ACCESS_TOKEN')
  Keyline.client(host: ENV['KEYLINE_API_HOST'], token: ENV['KEYLINE_ACCESS_TOKEN'])
end

require "pry"
Pry.start
